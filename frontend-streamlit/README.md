# IPTV Streamlit Admin UI

This Streamlit app is **admin/debug only**. It provides:

- Backend status panel
- Cache stats visualization
- Refresh button
- Raw payload debugging

## Run

```bash
streamlit run app.py
```

## Configuration

Update the API URL in the sidebar to point at the FastAPI backend.
